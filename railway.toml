[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "cd automation-system && gunicorn server:app --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
